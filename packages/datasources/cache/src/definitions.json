{
  "CacheHandler": {
    "additionalProperties": false,
    "type": "object",
    "title": "CacheHandler",
    "description": "Allows you to invalidate cache set by CacheTransform",
    "properties": {
      "getResolverName": {
        "type": "string",
        "description": "Name for query to get cache. Defaults to 'getCacheByKey'"
      },
      "deleteResolverName": {
        "type": "string",
        "description": "Name for mutation to invalidate cache. Defaults to 'deleteCacheByKey'"
      }
    }
  }
}
