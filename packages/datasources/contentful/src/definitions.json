{
  "ContentfulHandler": {
    "additionalProperties": false,
    "type": "object",
    "title": "ContentfulHandler",
    "description": "API-first content platform to build digital experiences",
    "properties": {
      "token": {
        "type": "string",
        "description": "Authentication token"
      },
      "endpoint": {
        "type": "string",
        "description": "A endpoint of your Contentful GraphQL API"
      }
    },
    "required": ["token", "endpoint"]
  }
}
