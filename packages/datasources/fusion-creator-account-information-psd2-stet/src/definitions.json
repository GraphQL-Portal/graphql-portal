{
  "FusionCreatorAccountInformationPSD2STETHandler": {
    "additionalProperties": false,
    "type": "object",
    "title": "FusionCreatorAccountInformationPSD2STETHandler",
    "description": "Retrieve set of account list, balances and account history e.g. for external aggregation purposes based on consent given by the customer. (PSD2 AISP scenario - STET standard)",
    "properties": {
      "authorizationHeader": {
        "type": "string",
        "description": "Authorization header to forward"
      }
    },
    "required": ["authorizationHeader"]
  }
}
