{
  "TwitterHandler": {
    "additionalProperties": false,
    "type": "object",
    "title": "TwitterHandler",
    "description": "An American microblogging and social networking service on which users post and interact with messages known as 'tweets'",
    "properties": {
      "token": {
        "type": "string",
        "description": "Your bearer token (wihout 'Bearer ' substring)"
      }
    },
    "required": ["token"]
  }
}
