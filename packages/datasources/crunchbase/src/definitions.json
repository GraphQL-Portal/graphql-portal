{
  "CrunchbaseHandler": {
    "additionalProperties": false,
    "type": "object",
    "title": "CrunchbaseHandler",
    "properties": {
      "userKey": {
        "type": "string",
        "description": "Used to validate each developer's access to the API and ensure that any rate limits or quotas are respected"
      }
    },
    "required": ["userKey"]
  }
}
