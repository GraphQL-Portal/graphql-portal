{
  "StripeHandler": {
    "additionalProperties": false,
    "type": "object",
    "title": "StripeHandler",
    "properties": {
      "token": {
        "type": "string",
        "description": "Authentication token"
      }
    },
    "required": ["token"]
  }
}
