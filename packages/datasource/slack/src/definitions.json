{
  "SlackHandler": {
    "additionalProperties": false,
    "type": "object",
    "title": "SlackHandler",
    "properties": {
      "token": {
        "type": "string",
        "description": "Token, which can be obtained here: https://api.slack.com/authentication"
      }
    },
    "required": ["token"]
  }
}
